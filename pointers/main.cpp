using namespace std;
#include <iostream>

int main(){
    /*

    // declaration

    int *p;
    cout << p << endl;          // 0x7f8aa61dba50 -> garbage
    cout << &p << endl;         // 0x7fffe2adf6f0 -> address in memory of pointer
    cout << sizeof p << endl;   // 4 (4 bytes, the size of an int)
    p = nullptr;
    cout << p << endl;          // 0

    // dereferencing

    int score {100};
    int *score_ptr {&score};

    cout << *score_ptr << endl;  // 100

    *score_ptr = 200;
    cout << *score_ptr << endl;  // 200
    cout << score << endl;       // 200

    // allocating memory during run time

    int *int_ptr {nullptr};
    int_ptr = new int;          // allocate an integer on the heap

    cout << int_ptr << endl;    // 0x558bec4a82b0
    cout << *int_ptr << endl;   // 0

    *int_ptr = 100;
    cout << *int_ptr << endl;   // 100

    int *int_ptr {nullptr};
    int_ptr = new int;           // allocate memory on the heap
    cout << int_ptr << endl;     // will display the memory address from the heap
    delete [] int_ptr;           // free storage

    // 

    size_t size {0};
    double *temp_ptr {nullptr};

    cout << "How many temps?" ;
    cin >> size;

    temp_ptr = new double[size];
    cout << temp_ptr << endl;

    delete [] temp_ptr;

    int array[] {1, 2, 3, 4, 5};
    int *pointer {array};
    int index {0};

    // same thing
    cout << array[index] << endl;
    cout << pointer[index] << endl;
    cout << *(array + index) << endl;
    cout << *(pointer + index) << endl;
    */

   

    return 0;
}